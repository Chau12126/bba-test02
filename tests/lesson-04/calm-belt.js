let navigatorName = 'Luffy';
let currentHakiLevel = 3;

const islandA = 15;
const islandB = 25;
const islandC = 40;

const sum = islandA + islandB +islandC
const average = sum/3
console.log (`Tổng lượng thịt tiêu thụ: ${sum}` );
console.log (`Trung bình lượng thịt tiêu thụ: ${average}` );

const residual = sum % 3;
console.log(`Số thịt còn dư khi đã chia đều: ${residual}`);
